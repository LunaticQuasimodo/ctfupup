# ROT13 Checker

Category: Reverse

This safe synthetic reverse fixture contains a simple checker implementation. The solver should derive the flag from code structure and verify it by running the checker function or reversing the transform.
