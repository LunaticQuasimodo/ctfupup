# XOR State

Category: Crypto

This safe synthetic challenge stores a ciphertext and repeating XOR key in `challenge.json`.
The goal is to demonstrate stateful crypto triage, evidence recording, and writeup generation.

No network access or brute force is required.
